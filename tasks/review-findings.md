# Code Review: Progression Engine (v6.5 Step 7)

## Review Date: 2026-03-09

## Files Reviewed

- `backend/workouts/models.py` (ProgressionProfile, ProgressionEvent, TrainingPlan FK, PlanSlot FK)
- `backend/workouts/services/progression_engine_service.py` (full file, 903 lines)
- `backend/workouts/serializers.py` (ProgressionProfileSerializer, ProgressionEventSerializer, ApplyProgressionInputSerializer)
- `backend/workouts/views.py` (ProgressionProfileViewSet, PlanSlot actions)
- `backend/workouts/urls.py` (route registration)
- `backend/workouts/migrations/0025_progression_engine.py`
- `backend/workouts/management/commands/seed_progression_profiles.py`

---

## Critical Issues (must fix before merge)

| #   | File:Line                               | Issue                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Suggested Fix                                                                                                                                                                               |
| --- | --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | `views.py:4332-4338`                    | **Trainees can self-apply progression with arbitrary overrides.** The auth check `if user.role == 'TRAINEE' and user.pk != trainee.pk` only prevents trainee A from applying to trainee B's slot. A trainee can call `apply-progression` on their own slot and use `override_sets`, `override_reps_min`, `override_reps_max`, `override_load` to set arbitrary values, effectively bypassing their trainer's programming. This is an authorization gap — progression application should be a trainer/admin-only action.                                                                                                                              | Add `if user.role == 'TRAINEE': raise PermissionDenied("Only trainers and admins can apply progression.")` at the top of the action.                                                        |
| C2  | `progression_engine_service.py:848-851` | **DecisionLog actor_type is never SYSTEM.** The logic `DecisionLog.ActorType.USER if actor_id else DecisionLog.ActorType.SYSTEM` — but `actor_id` is always `user.pk` (never None) because the view always passes `actor_id=user.pk`. System-triggered progressions (e.g., if a batch/cron job calls `apply_progression`) would be incorrectly tagged as USER because there's no code path that passes `actor_id=None`. Additionally, the `actor` FK field on DecisionLog has a separate `actor_id` Django auto-field, and passing `actor_id=actor_id` to `create()` sets the FK via the auto-field — this works but the actor_type logic is broken. | Either accept that all current calls are user-triggered and hardcode `ActorType.USER`, or add a separate `actor_type` parameter to `apply_progression()` for future system-triggered calls. |
| C3  | `progression_engine_service.py:838-845` | **`load_prescription_pct` is never cleared.** Line 841: `if prescription.load_percentage: slot.load_prescription_pct = prescription.load_percentage` — when `load_percentage` is `None` or `Decimal('0')` (e.g., for rep_staircase, double_progression, linear where percentages don't apply), the old percentage is silently preserved. The field is still listed in `update_fields` so it gets saved with its stale value. When switching from a percent-based profile (staircase) to an absolute-load profile (linear), the slot retains the old percentage, creating conflicting prescription data.                                              | Change to unconditional assignment: `slot.load_prescription_pct = prescription.load_percentage` (which allows setting to None).                                                             |

---

## Major Issues (should fix)

| #   | File:Line                                                           | Issue                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Suggested Fix                                                                                                                                                                                     |
| --- | ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | `progression_engine_service.py:603-609`                             | **Wave cycle position tracks ALL event types, not just progressions.** `progression_count = len(events)` counts deload, failure, hold, and progression events equally. A deload event advances the wave counter, so after Accumulation + auto-deload-due-to-gap, the system thinks it's at Build (week 3) instead of Build (week 2). This will produce incorrect wave positions. Compare with staircase_percent (line 223) which correctly filters `event_type == 'progression'`. | Filter: `progression_count = len([e for e in events if e.event_type in ('progression', 'deload')])` or add a dedicated `wave_week` counter field.                                                 |
| M2  | `views.py:4349-4362`                                                | **Frozen dataclass mutation via `__dict__` spread is fragile.** `NextPrescription(**{**prescription.__dict__, 'sets': overrides['override_sets']})` works today but breaks if the frozen dataclass ever uses `__slots__` (common for frozen dataclasses). This pattern is also verbose — 4 separate conditionals each creating a new object.                                                                                                                                      | Use `dataclasses.replace(prescription, sets=overrides['override_sets'])` which is the idiomatic, safe approach for frozen dataclass copies. Combine all overrides into a single `replace()` call. |
| M3  | `progression_engine_service.py:332-335`                             | **Lower body detection is too restrictive.** Only `primary_compound` slot role qualifies for the lower body load increment. A Romanian deadlift assigned as `secondary_compound` targeting `hamstrings` would get the upper body increment (5lb instead of 10lb). The hardcoded muscle group tuple may also miss values depending on the `DetailedMuscleGroup` enum (e.g., `'lower_back'` vs `'spinal_erectors'`).                                                                | Broaden to include `secondary_compound` slot role. Cross-reference the tuple against the actual `DetailedMuscleGroup` enum values to ensure completeness.                                         |
| M4  | `progression_engine_service.py:111-124`                             | **`_get_recent_sets` has no hard limit on returned rows.** A heavy user logging 10 sets/exercise/session, 4 sessions/week, for 4 weeks = 160 rows. While manageable, there's no protection against pathological data (e.g., bulk import of historical data). The returned list is iterated multiple times (grouping, completion checks, failure counting).                                                                                                                        | Add a hard `[:500]` or similar cap to the queryset.                                                                                                                                               |
| M5  | `progression_engine_service.py:237,264,289,357,454,510,544,566,618` | **Hardcoded `load_unit = 'lb'` throughout all evaluators.** No support for kilograms. Users in metric countries will receive prescriptions in pounds. The `LiftSetLog` model has `LoadUnit` choices of 'lb' and 'kg', and `LiftMax` stores loads in whatever unit the user uses.                                                                                                                                                                                                  | Read the unit from the `LiftMax` record or propagate from `LiftSetLog` entries. Fall back to 'lb' only if no data exists.                                                                         |
| M6  | `progression_engine_service.py:260`                                 | **Off-by-one in staircase step calculation.** `new_pct = start_pct + (step_pct * (current_step + 1))` — on the first work week (`current_step=0`), this gives `start_pct + step_pct * 1 = 77.5%` with default config. The trainee never trains at the configured `start_pct` (75%). The step should start at 0: `start_pct + step_pct * current_step`.                                                                                                                            | Change to `step_pct * current_step` so first week uses `start_pct`.                                                                                                                               |
| M7  | `models.py:2776`                                                    | **`ProgressionEvent.reason_codes` uses `JSONField` while `DecisionLog.reason_codes` uses `ArrayField`.** These are semantically identical fields. The inconsistency means different query patterns (`__contains` for ArrayField vs `__contains` with different semantics for JSONField) and could confuse developers.                                                                                                                                                             | Use `ArrayField` for consistency with `DecisionLog`, or document the divergence.                                                                                                                  |
| M8  | `views.py:4349`                                                     | **Truthiness check on optional integer override.** `if overrides.get('override_sets')` is falsy for value `0`. While the serializer's `min_value=1` prevents 0, the same pattern is used for `override_load` (line 4361: `if overrides.get('override_load') is not None`) which correctly uses `is not None`. The inconsistency is a code smell and future bug vector if min_value is changed.                                                                                    | Use `if 'override_sets' in overrides` or `if overrides.get('override_sets') is not None` consistently for all override fields.                                                                    |

---

## Minor Issues (nice to fix)

| #   | File:Line                                | Issue                                                                                                                                                                                                                                                                                                                   | Suggested Fix                                                                                                                                                                                                                               |
| --- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| m1  | `progression_engine_service.py:56,66,82` | **Mutable `list` in frozen dataclass fields.** `reason_codes: list[str]` and `blockers: list[str]` on frozen dataclasses allow mutation via `.append()` despite the frozen flag.                                                                                                                                        | Use `tuple[str, ...]` for truly immutable frozen dataclasses.                                                                                                                                                                               |
| m2  | `views.py:4296-4313,4369-4381,4395-4403` | **Manual dict construction repeated 4 times for dataclass serialization.** Each action manually constructs a response dict from the dataclass fields.                                                                                                                                                                   | Create a helper `_prescription_to_dict(p: NextPrescription) -> dict` or use `dataclasses.asdict()` with custom Decimal handling.                                                                                                            |
| m3  | `progression_engine_service.py:175-180`  | **`_avg_rpe` returns Decimal with potentially unbounded precision.** `sum(rpe_values) / len(rpe_values)` for Decimal division can produce long decimal expansions.                                                                                                                                                      | Add `.quantize(Decimal('0.1'))` to round to 1 decimal place.                                                                                                                                                                                |
| m4  | `seed_progression_profiles.py:133`       | **`\*args: object, **options: object` type hints are overly generic.\*\*                                                                                                                                                                                                                                                | Use `*args: Any, **options: Any` to match Django's management command signature.                                                                                                                                                            |
| m5  | `serializers.py:1596-1666`               | **Serializers use `ModelSerializer` not `rest_framework_dataclasses`.** Per project rule in `.claude/rules/datatypes.md`, API responses should use `rest_framework_dataclasses`. However, no existing serializers in the codebase use this pattern, so this appears to be a codebase-wide gap rather than a regression. | Low priority — note for future migration.                                                                                                                                                                                                   |
| m6  | `progression_engine_service.py:219-222`  | **ProgressionEvent queries inside evaluators are not prefetched.** Staircase (line 219) and wave (line 603) evaluators query ProgressionEvent within the evaluator function. For batch operations (computing prescriptions for all slots in a plan), each slot triggers a separate DB query.                            | Accept for now; if batch operations are added later, prefetch events and pass them into evaluators.                                                                                                                                         |
| m7  | `views.py:4323-4325`                     | **Import inside method.** `from .services.progression_engine_service import ...` is done inside each action method (4 times). While this avoids circular imports, the same import is already at module level (line 137).                                                                                                | Remove the in-method imports since the module-level import of `NextPrescription` already exists. Add `compute_next_prescription`, `apply_progression`, `evaluate_progression_readiness`, `get_progression_history` to the top-level import. |

---

## Security Concerns

1. **C1 (Critical):** Trainees can call `apply-progression` on their own slots with arbitrary override values, bypassing trainer-prescribed programming. This is the most significant authorization gap.
2. **No rate limiting** on `apply-progression` — a bad actor could spam this endpoint, creating hundreds of ProgressionEvent + DecisionLog rows.
3. **No validation that `progression_profile` FK belongs to accessible scope** when assigning to a PlanSlot or TrainingPlan via update. A trainer could assign another trainer's custom profile by UUID if they know/guess the UUID.

## Performance Concerns

1. **M4:** `_get_recent_sets` has no hard limit — pathological data could return thousands of rows.
2. **m6:** ProgressionEvent queries inside evaluators create N+1 pattern for batch operations.
3. **`_get_effective_profile` traverses 3 FKs** (`slot.progression_profile`, `slot.session.week.plan.default_progression_profile`) — covered by `select_related` in the ViewSet but any direct service caller would trigger lazy loads.

---

## Acceptance Criteria Verification

| Criterion                                                | Status  | Notes                                                                                                                                                     |
| -------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ProgressionProfile model with all required fields        | PASS    | UUID PK, 5 types, rules/deload/failure JSON, is_system, created_by                                                                                        |
| progression_type choices (5 types)                       | PASS    | staircase_percent, rep_staircase, wave_by_month, double_progression, linear                                                                               |
| TrainingPlan.default_progression_profile FK              | PASS    | Nullable, SET_NULL                                                                                                                                        |
| PlanSlot.progression_profile FK (override)               | PASS    | Nullable, SET_NULL                                                                                                                                        |
| ProgressionEvent model with full audit trail             | PASS    | UUID PK, trainee FK, exercise FK, plan_slot FK, event_type, old/new prescription, reason_codes, decision_log FK                                           |
| All models use UUID primary keys                         | PASS    |                                                                                                                                                           |
| Staircase Percent rules                                  | PARTIAL | Off-by-one on start step (M6) — first week starts at 77.5% instead of 75%                                                                                 |
| Rep Staircase rules                                      | PASS    | Hold load, climb reps, bump load at top rung                                                                                                              |
| Double Progression rules                                 | PASS    | Earn reps in range, increase load when all sets hit top                                                                                                   |
| Linear rules                                             | PASS    | Fixed increment, deload on consecutive failures                                                                                                           |
| Wave-by-Month rules                                      | PARTIAL | Wave position tracking counts all event types (M1)                                                                                                        |
| Auto-progression gated by completion, effort, pain flags | PARTIAL | Completion and effort (RPE) checked. Pain flags not checked anywhere.                                                                                     |
| compute_next_prescription()                              | PASS    | Returns NextPrescription dataclass                                                                                                                        |
| evaluate_progression_readiness()                         | PASS    | Returns ProgressionReadiness with blockers                                                                                                                |
| apply_progression()                                      | PASS    | Creates ProgressionEvent + DecisionLog, updates slot                                                                                                      |
| get_progression_history()                                | PASS    | With select_related, capped at 50                                                                                                                         |
| All decisions logged via DecisionLog                     | PASS    |                                                                                                                                                           |
| CRUD for ProgressionProfile                              | PASS    | Full CRUD with role-based security                                                                                                                        |
| GET /plan-slots/{id}/next-prescription/                  | PASS    |                                                                                                                                                           |
| POST /plan-slots/{id}/apply-progression/                 | PARTIAL | Auth gap (C1) — trainees can self-apply with overrides                                                                                                    |
| GET /plan-slots/{id}/progression-history/                | PASS    |                                                                                                                                                           |
| Row-level security on all endpoints                      | PARTIAL | ProgressionProfile has proper role filtering. PlanSlot actions inherit get_queryset security. But apply-progression allows trainee self-application (C1). |
| 5 system seed profiles                                   | PASS    |                                                                                                                                                           |
| No LiftMax blocker                                       | PASS    | Returns hold with 'no_max'                                                                                                                                |
| No history blocker                                       | PASS    | Returns hold with 'no_history'                                                                                                                            |
| Gap > 14 days deload                                     | PASS    | 90% TM                                                                                                                                                    |
| Consecutive failures trigger deload                      | PASS    | Configurable per profile                                                                                                                                  |
| Slot profile overrides plan default                      | PASS    | `_get_effective_profile` resolves correctly                                                                                                               |
| Deload week blocker                                      | PASS    | Checked in `evaluate_progression_readiness`                                                                                                               |

---

## Quality Score: 6/10

## Recommendation: REQUEST CHANGES

### Summary

The progression engine is well-architected: clean evaluator dispatch pattern, proper use of frozen dataclasses for return types, good separation of concerns (service does computation, view handles HTTP), and solid edge case coverage for gaps, failures, and deloads. The 5 progression types are implemented with reasonable fidelity to the spec.

However, 3 critical issues must be fixed: (C1) trainees can self-apply progression with arbitrary overrides — a clear authorization gap; (C2) DecisionLog actor_type is never SYSTEM due to faulty conditional; (C3) `load_prescription_pct` is never cleared when switching progression types. Major issues include the wave cycle counter bug (M1), staircase off-by-one (M6), hardcoded 'lb' unit (M5), and the missing pain flag gate from the acceptance criteria. These must be addressed before merge.
